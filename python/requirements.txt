# Core simulation and math
mujoco>=3.4.0
numpy>=2.4.2
scipy>=1.17.0
matplotlib>=3.10.8

# Reinforcement learning / parameter optimisation
gymnasium>=1.1.0
stable-baselines3>=2.6.0
cma>=4.0.0

# Terrain generation (heightmap images) for python/sim/go2/
opencv-python>=4.10.0

# Spiking-neuron CPG experiments in python/cpg/brian_sim/ only.
# Not needed to run the locomotion simulation.
Brian2>=2.10.1

# Terminal-UI launcher (scripts/interface_app.py) only.
# POSIX-only: depends on termios, so it does not run on Windows.
simple-term-menu>=1.6.4 ; sys_platform != "win32"
