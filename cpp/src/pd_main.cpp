/**
 * @file pd_main.cpp
 * @brief Entry point for the PD (proportional-derivative) controller demo executable.
 *
 * Scaffolding stub: currently prints a startup banner. Intended to grow into
 * a standalone demo of the joint-level PD controller used as the baseline
 * against the OIAC controllers.
 */
#include <print>

/**
 * @brief Program entry point for the PD controller demo executable.
 * @return Process exit code (0 on success).
 */
int main(){
    std::println("Hello from PD controller!");
    return 0;
}