/**
 * @file cpg_main.cpp
 * @brief Entry point for the central pattern generator (CPG) demo executable.
 *
 * Scaffolding stub: currently prints a startup banner. Intended to grow into
 * a standalone demo of the CPG that produces the rhythmic phase signals
 * driving the quadruped's gaits.
 */
#include <print>

/**
 * @brief Program entry point for the CPG demo executable.
 * @return Process exit code (0 on success).
 */
int main(){
    std::println("Hello from cpg main!");
    return 0;
}